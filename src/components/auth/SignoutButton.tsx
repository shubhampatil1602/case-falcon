import { Button } from '../ui/button';

const SignoutButton = () => {
  return (
    <form>
      <Button type='submit' size='sm' variant='ghost'>
        Sign out
      </Button>
    </form>
  );
};

export default SignoutButton;
